# Configuración de Azure Pipeline para pnpm

# Directorio de store de pnpm
# Se utiliza para el cache en el pipeline
store-dir=~/.pnpm-store

# Usar lockfile estricto (no modificar pnpm-lock.yaml)
frozen-lockfile=true

# Verificación de integridad de paquetes
package-import-method=auto
